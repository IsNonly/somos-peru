node_modules/
dist/
.vercel

.env
.env.*
!.env.example

# Credenciales locales de cada instancia (una por provincia), pasadas por chat
# y usadas solo por los scripts de bootstrap. Nunca van al repo.
scripts/.instancias/
*.local
.DS_Store
.claude/

# Media pesada suelta: no va al repo (el video/PDF de capacitación ahora se
# sirven desde Supabase Storage, ver apps/registro/public/videos/README.md)
*.mp4
*.mov
*.webm
*.zip
*.pdf
CALI.xlsx

.vercel
.env*
supabase/.temp/
